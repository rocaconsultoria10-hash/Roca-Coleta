import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0B2D5C",
    },
    secondary: {
      main: "#00AEEF",
    },
    background: {
      default: "#F5F7FA",
    },
  },
});

export default theme;